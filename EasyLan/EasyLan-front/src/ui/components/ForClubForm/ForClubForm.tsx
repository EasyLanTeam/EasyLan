import * as React from 'react';

interface IForClubFormProps {
}

type ForClubFormValues = {
  username: string;
  email: string;
  password: string;
  confirmPassword: string;
};

// const formInitialValues: ForClubFormValues = {
// };

const ForClubForm: React.FunctionComponent<IForClubFormProps> = (props) => {
  return null;
};

export default ForClubForm;
